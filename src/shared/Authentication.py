import jwt 

class Auth:
    """
    Auth Class
    """

    @staticmethod
    def gen_token(user_id) :
        """
        Generate Token
        """
        try:
            payload = {
                    }
